//! SPL (Spec Layer) parser and type checker.
//!
//! Handles parsing of `.spl` files, name resolution,
//! type checking, and semantic analysis.

pub mod ast;
pub mod lexer;
pub mod parser;
// pub mod resolve;
// pub mod typecheck;
